[FROM: P0 -- Master Strategic Plan] [DATE: 2026-04-19] [TOPIC: GOV-021 v1.1 landed; P4-003 pilot DEPLOYMENT.md live; Jason assigned to four [NEEDS INPUT] gaps]
[ORIGIN: Work produced in P0 session on 2026-04-19 by Gregory Bernardo (GOV-021 authoring and v1.1 patch series). Retained per GOV-007 §3.5 audit-trail convention.]

---

**Status:** APPROVED
**Approver:** Gregory Bernardo
**Filed:** 2026-04-19
**Receiving project:** P4 (AI Infrastructure & Deployment) -- Jason Harris
**Related initiatives:** I-73 (P4-003 Ph2 ownership transfer), I-48 (API-Driven Hub), I-65 (RAG corpus on DGX Spark), I-70 (RevitPrint Minion CI/CD)
**Source-of-truth commits:**
- PR #1 merge: `da09503` (GOV-021 v1.0 + registry + GOV-014 cross-ref + pilot DEPLOYMENT.md + BCG-Greg/bcg-easybutton deprecation)
- v1.1 patch series: `5a777ce` (script `/MIR` -> `/E /XO`), `f5d4ab2` (GOV-021 v1.1), `4650844` (registry v3.4), `6da666b` (JSON v1.4)

---

## 1. Summary

GOV-021 Artifact Deployment Routing Standard is live at v1.1. The standard
declares canonical/mirror routing for every artifact class BCG produces
(8 groups A-H, 30+ classes) and introduces the per-project `DEPLOYMENT.md`
convention. P4-003 (`bcgcorp/bcg-easybutton`) is the pilot adopter.

Four [NEEDS INPUT] gaps are routed to you for v1.2 closure (§4).
Two informational items affect your active P4 work streams (§3).

## 2. What changed

### Governance

- **GOV-021 v1.0 approved** 2026-04-19 (PR #1, Copilot-reviewed); **v1.1**
  same-day clarification added sync-mode vocabulary
  (exact / additive / snapshot) and corrected §8.1 to match the revised
  `scripts/sync-a-to-b.ps1` (`/E /XO` additive, not `/MIR` destructive).
- **BCG_Governance_Doc_Registry.md** bumped v3.2 -> v3.4 across two
  commits. Runtime-fetched doc count 19 -> 20, total 24 -> 25.
- **BCG_Document_Registry.json** bumped v1.2 -> v1.4.
- **GOV-014 v1.3 -> v1.4** with a scope note in §3 pointing to GOV-021
  for non-document artifact classes.

### Tooling

- **`scripts/sync-a-to-b.ps1`** shipped in governance repo. Hourly Task
  Scheduler-driven `A:\ -> B:\` additive copy for status-update prompts
  (class A3). B:\ is now a superset of A:\, not an exact mirror -- no
  content is ever deleted from B:\.
- **`last-sync.txt` contract** at `C:\ProgramData\BCG\sync-a-to-b\` drives
  stale-mirror detection in the W-20 Checklist 4D finishing-work check.

### Pilot

- **`bcgcorp/bcg-easybutton/DEPLOYMENT.md`** created as the pilot
  per-project manifest per GOV-021 §6. Declares C4, C6, C7, E1, E1b,
  E1c, E1d, H1, and A3 routing for P4-003.
- **`bcgcorp/bcg-easybutton/.windsurfrules`** bumped v1.0 -> v1.1 to
  reflect A:\ canonical for Cascade-session governance artifacts and
  add the end-of-session commit rule (GOV-021 §10 Rule 4).

### Cleanup

- **`BCG-Greg/bcg-easybutton`** deprecated -- README replaced with
  redirect to the canonical org repo. Archive toggle flip pending
  Gregory's manual action in GitHub Settings.

## 3. Informational items affecting your active work

### 3.1 Status-update prompts for P4-001, P4-002, P4-003 now route to A:\

Per GOV-021 §3.1 class A3 and §5.2 active migration row, the canonical
location for Claude Projects status-update prompts is
`A:\AI Accessible\Outputs\P4-XXX\`. B:\ is an auto-maintained additive
copy (hourly sync from A:\). Do not write to B:\ directly -- the
`.windsurfrules` §8 in the P4-003 repo now explicitly forbids it as a
destination (invariant list).

If you lack A:\ access on any of your capture devices, flag via normal
P4 channels so Gregory can grant access. Once all P4 contributors have
A:\ access, the B:\ auto-copy will be retired (that's the §5.2 flip
trigger for A3).

### 3.2 C1 / C4 GitHub -> GitLab migration flip trigger formalized

Per GOV-021 §5.2, the flip from GitHub to GitLab (`alexandria.bcg-corp.com`)
for `bcg-ops-governance` (C1) and `bcg-easybutton` (C4) is gated on
**claude.ai GitLab connector GA**. Until GA:

- GitHub remains canonical for both repos
- No dual-write to GitHub and GitLab (§10 Rule 3)
- Optional one-way read-only mirror GitHub -> GitLab for visibility is
  acceptable if you want it, but not required

When GA lands, the flip is a dedicated handoff with a migration plan.
Do not cutover preemptively.

## 4. Action items for P4 (target: GOV-021 v1.2)

GOV-021 §9 explicitly tags 17 `[NEEDS INPUT]` gaps. Four are assigned to
you. Target version for closure is v1.2; no hard deadline.

### 4.1 [GAP C8] Windsurf rebuild prompts -- canonical path convention

- **Status:** Not declared. GOV-017 §5 says "alongside code" but doesn't
  pin the exact path (repo subfolder? filename pattern?).
- **Needed:** A committed path convention for P4-002 rebuild prompts that
  can be declared in GOV-021 §3.3 class C8 row as Stable.
- **Co-owner:** Gregory (if you want a joint answer on naming).

### 4.2 [GAP D1] ClarkKent Docker Compose stack -- source of truth

- **Status:** `D:\observability` on the ClarkKent VM is the deploy
  location, but the repo-of-record for the compose files, container
  names, volume paths, and provisioning directory are undocumented per
  Infrastructure Inventory v1.5 §23 Gap #10.
- **Needed:** One canonical answer (repo? where?) so the operational
  stack is reproducible from declarative source, not tribal knowledge.
- **Co-owner:** Bob (for the infrastructure layer).

### 4.3 [GAP D2] Grafana dashboards -- source of truth

Tied to D1. Once D1 lands a repo-of-record, Grafana dashboards
(file-provisioned) inherit the same routing.

### 4.4 [GAP E4] I-48 API-Driven Hub -- output destination(s)

- **Status:** I-48 API-Driven Hub is operational (superseded I-30 email
  pipeline), but the destination paths for the P0 sync outputs are not
  declared in Infrastructure Inventory or the Initiative Catalog.
- **Needed:** Per-output-type destination(s) in a form that can be a
  GOV-021 §3.5 class E4 row.

## 5. Non-actions / out-of-scope

- **Do NOT implement** the GitHub -> GitLab flip (§3.2) until the GA
  trigger condition is met
- **Do NOT create** a DEPLOYMENT.md for `bcg-ops-revit-tools` yet -- that
  repo's migration to GitLab is post-GA; manifest work on it waits
- **Do NOT write** to `B:\AI Accessible\Outputs\P4-*\` directly for
  Cascade-session artifacts (write to A:\ only; B:\ is auto-populated)

## 6. Source-of-truth pointers

- `bcgcorp/bcg-ops-governance/standards/BCG_Artifact_Deployment_Routing_Standard.md` v1.1
- `bcgcorp/bcg-ops-governance/standards/BCG_Governance_Doc_Registry.md` v3.4 §3.20
- `bcgcorp/bcg-ops-governance/standards/BCG_Document_Registry.json` v1.4
- `bcgcorp/bcg-ops-governance/scripts/sync-a-to-b.ps1`
- `bcgcorp/bcg-easybutton/DEPLOYMENT.md`
- `bcgcorp/bcg-easybutton/.windsurfrules` v1.1

## 7. Response protocol

Acknowledge receipt at your next P4 working session. The four action
items (§4) have no hard deadline; target closure by GOV-021 v1.2. When
you have an answer for any single gap, submit a PR against
`BCG_Artifact_Deployment_Routing_Standard.md` with the row update; no
need to wait for all four.

If any item here reframes scope or surfaces a dependency I missed, route
it back as a `[FROM: P4] [TO: P0]` handoff and we re-plan.

---

*Filed per GOV-007 §3 handoff protocol and §3.5 Handoff Validation Protocol.
Log entry appended to GOV-007 §7 in the same commit.*
